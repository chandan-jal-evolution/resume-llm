import { ChatWidget } from "@/components/component/chat-widget";

export const dynamic = "force-dynamic";

export default function Home() {
  // const res = await getDescriptionFromImage();
  // console.log(res);

  return <ChatWidget />;
}
